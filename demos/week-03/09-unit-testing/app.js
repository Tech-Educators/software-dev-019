//we need to export this function to import it into a different file
export function sum(a, b) {
  return a + b;
}

export function multiply(a, b) {
  return a * b;
}

//I will be running my unit tests in a separate file called fileName.test.js

//you can store types on a separate file, and import them when you need them
//this way, they are reusable

// export type myJobType = {
//   company?: string;
//   location?: string;
//   role?: string;
// };

//Note: what if I have a type declared, but I am not going to use all the properties in the actual object? --> optional chaining (?.)
// console.log(myOtherJob?.role);
